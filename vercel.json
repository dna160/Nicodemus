{
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "export NODE_ENV=production && cd packages/shared && pnpm run build && cd ../../apps/web && pnpm run build"
}
